{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "socket.io",
            "program": "${workspaceFolder}/webSocketServer/runWebSocketServer.js"
        },

    ]
}